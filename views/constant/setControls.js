const SET_CONTROL_KNOB_VIEW = `
Turn the knob you'll use to make selections.
`;

const SET_CONTROL_BUTTON_VIEW = `
Tap the button you'll use to make selections.
`;

const SET_TRANSPORT_BUTTON_VIEW = `
Tap the button you'll use to control transport.
`;

const SET_RECORD_BUTTON_VIEW = `
Tap the button you'll use to control recording.
`;

const setControls = {
  SET_CONTROL_KNOB_VIEW,
  SET_CONTROL_BUTTON_VIEW,
  SET_TRANSPORT_BUTTON_VIEW,
  SET_RECORD_BUTTON_VIEW,
};

module.exports = setControls;
